  
  var slider = tns({
    container: '.my-slider',
    items: 1,
    controls: false,
    nav: false,
    // autoplay: true,
    // autoplayButton: false,
    // edgePadding: 50,
    center: true,
  });

  var slider = tns({
    container: '.my-slider-mobile',
    items: 3,
    controls: false,
    nav: false,
    // axis: vertical,
    // autoplay: true,
    // autoplayButton: false,
    // edgePadding: 50,
    center: true,
  });